import Quick
import Nimble

class ViewControllerSpec: QuickSpec {
    override func spec() {
        describe("a state") {
            it("should pass") {
                expect(1).to(equal(1))
            }
        }
    }
}
